{
  "name": "attestation-js-policy-example",
  "version": "1.0.0",
  "description": "In-toto attestation validation policy example",
  "scripts": {
    "build": "node esbuild.js && extism-js dist/policy.js -i policy.d.ts -o policy.wasm"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
