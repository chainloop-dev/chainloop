Set of reference CI workflow examples that showcase how the attestation process can be embedded.

NOTE: You do not need to have one of these Runner types to use Chainloop. Chainloop is CI runner agnostic.
