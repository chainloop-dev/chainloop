apiVersion: workflowcontract.chainloop.dev/v1
kind: Policy
metadata:
  name: workflow-push-only
spec:
  policies:
    - kind: ATTESTATION
      path: workflow.rego
      attestation_phases:
        - PUSH
