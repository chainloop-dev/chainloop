{{- if .Values.controlplane.ingressAPI.enabled -}}
{{- $fullName := printf "%s-%s" (include "chainloop.controlplane.fullname" .) "grpc" -}}
{{- $svcPort := .Values.controlplane.serviceAPI.port -}}
apiVersion: {{ include "common.capabilities.ingress.apiVersion" . }}
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "chainloop.controlplane.labels" . | nindent 4 }}
  {{- with .Values.controlplane.ingressAPI.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.controlplane.ingressAPI.className (eq "true" (include "common.ingress.supportsIngressClassname" .)) }}
  ingressClassName: {{ .Values.controlplane.ingressAPI.className | quote }}
  {{- end }}
  {{- if .Values.controlplane.ingressAPI.tls }}
  tls:
    {{- range .Values.controlplane.ingressAPI.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.controlplane.ingressAPI.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if eq "true" (include "common.ingress.supportsPathType" $) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend: {{- include "common.ingress.backend" (dict "serviceName" $fullName "servicePort" $svcPort "context" $)  | nindent 14 }}
          {{- end }}
    {{- end }}
{{- end }}
