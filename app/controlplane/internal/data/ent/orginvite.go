// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/chainloop-dev/chainloop/app/controlplane/internal/biz"
	"github.com/chainloop-dev/chainloop/app/controlplane/internal/data/ent/organization"
	"github.com/chainloop-dev/chainloop/app/controlplane/internal/data/ent/orginvite"
	"github.com/chainloop-dev/chainloop/app/controlplane/internal/data/ent/user"
	"github.com/google/uuid"
)

// OrgInvite is the model entity for the OrgInvite schema.
type OrgInvite struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// ReceiverEmail holds the value of the "receiver_email" field.
	ReceiverEmail string `json:"receiver_email,omitempty"`
	// Status holds the value of the "status" field.
	Status biz.OrgInviteStatus `json:"status,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// DeletedAt holds the value of the "deleted_at" field.
	DeletedAt time.Time `json:"deleted_at,omitempty"`
	// OrganizationID holds the value of the "organization_id" field.
	OrganizationID uuid.UUID `json:"organization_id,omitempty"`
	// SenderID holds the value of the "sender_id" field.
	SenderID uuid.UUID `json:"sender_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the OrgInviteQuery when eager-loading is set.
	Edges        OrgInviteEdges `json:"edges"`
	selectValues sql.SelectValues
}

// OrgInviteEdges holds the relations/edges for other nodes in the graph.
type OrgInviteEdges struct {
	// Organization holds the value of the organization edge.
	Organization *Organization `json:"organization,omitempty"`
	// Sender holds the value of the sender edge.
	Sender *User `json:"sender,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// OrganizationOrErr returns the Organization value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e OrgInviteEdges) OrganizationOrErr() (*Organization, error) {
	if e.loadedTypes[0] {
		if e.Organization == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: organization.Label}
		}
		return e.Organization, nil
	}
	return nil, &NotLoadedError{edge: "organization"}
}

// SenderOrErr returns the Sender value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e OrgInviteEdges) SenderOrErr() (*User, error) {
	if e.loadedTypes[1] {
		if e.Sender == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: user.Label}
		}
		return e.Sender, nil
	}
	return nil, &NotLoadedError{edge: "sender"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*OrgInvite) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case orginvite.FieldReceiverEmail, orginvite.FieldStatus:
			values[i] = new(sql.NullString)
		case orginvite.FieldCreatedAt, orginvite.FieldDeletedAt:
			values[i] = new(sql.NullTime)
		case orginvite.FieldID, orginvite.FieldOrganizationID, orginvite.FieldSenderID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the OrgInvite fields.
func (oi *OrgInvite) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case orginvite.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				oi.ID = *value
			}
		case orginvite.FieldReceiverEmail:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field receiver_email", values[i])
			} else if value.Valid {
				oi.ReceiverEmail = value.String
			}
		case orginvite.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				oi.Status = biz.OrgInviteStatus(value.String)
			}
		case orginvite.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				oi.CreatedAt = value.Time
			}
		case orginvite.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				oi.DeletedAt = value.Time
			}
		case orginvite.FieldOrganizationID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field organization_id", values[i])
			} else if value != nil {
				oi.OrganizationID = *value
			}
		case orginvite.FieldSenderID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field sender_id", values[i])
			} else if value != nil {
				oi.SenderID = *value
			}
		default:
			oi.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the OrgInvite.
// This includes values selected through modifiers, order, etc.
func (oi *OrgInvite) Value(name string) (ent.Value, error) {
	return oi.selectValues.Get(name)
}

// QueryOrganization queries the "organization" edge of the OrgInvite entity.
func (oi *OrgInvite) QueryOrganization() *OrganizationQuery {
	return NewOrgInviteClient(oi.config).QueryOrganization(oi)
}

// QuerySender queries the "sender" edge of the OrgInvite entity.
func (oi *OrgInvite) QuerySender() *UserQuery {
	return NewOrgInviteClient(oi.config).QuerySender(oi)
}

// Update returns a builder for updating this OrgInvite.
// Note that you need to call OrgInvite.Unwrap() before calling this method if this OrgInvite
// was returned from a transaction, and the transaction was committed or rolled back.
func (oi *OrgInvite) Update() *OrgInviteUpdateOne {
	return NewOrgInviteClient(oi.config).UpdateOne(oi)
}

// Unwrap unwraps the OrgInvite entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (oi *OrgInvite) Unwrap() *OrgInvite {
	_tx, ok := oi.config.driver.(*txDriver)
	if !ok {
		panic("ent: OrgInvite is not a transactional entity")
	}
	oi.config.driver = _tx.drv
	return oi
}

// String implements the fmt.Stringer.
func (oi *OrgInvite) String() string {
	var builder strings.Builder
	builder.WriteString("OrgInvite(")
	builder.WriteString(fmt.Sprintf("id=%v, ", oi.ID))
	builder.WriteString("receiver_email=")
	builder.WriteString(oi.ReceiverEmail)
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", oi.Status))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(oi.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("deleted_at=")
	builder.WriteString(oi.DeletedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("organization_id=")
	builder.WriteString(fmt.Sprintf("%v", oi.OrganizationID))
	builder.WriteString(", ")
	builder.WriteString("sender_id=")
	builder.WriteString(fmt.Sprintf("%v", oi.SenderID))
	builder.WriteByte(')')
	return builder.String()
}

// OrgInvites is a parsable slice of OrgInvite.
type OrgInvites []*OrgInvite
