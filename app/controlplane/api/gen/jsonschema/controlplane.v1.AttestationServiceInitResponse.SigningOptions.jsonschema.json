{
  "$id": "controlplane.v1.AttestationServiceInitResponse.SigningOptions.jsonschema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "patternProperties": {
    "^(signing_ca)$": {
      "description": "If set, the attestation wil be signed with ephemeral certificates issued by this CA",
      "type": "string"
    },
    "^(timestamp_authority_url)$": {
      "description": "TSA service to be used for signing",
      "type": "string"
    }
  },
  "properties": {
    "signingCa": {
      "description": "If set, the attestation wil be signed with ephemeral certificates issued by this CA",
      "type": "string"
    },
    "timestampAuthorityUrl": {
      "description": "TSA service to be used for signing",
      "type": "string"
    }
  },
  "title": "Signing Options",
  "type": "object"
}
