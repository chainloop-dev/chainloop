# Contract for chainloop-vault-scorecards workflow
schemaVersion: v1
runner:
  type: GITHUB_ACTION
materials:
  - type: SARIF
    name: sarif-results
    output: true
policies:
  attestation:
    - ref: source-commit
