# Contract for the chainloop-vault-build-and-package workflow
schemaVersion: v1
policies:
  attestation:
    - ref: source-commit
      with:
        check_signature: yes
    - ref: containers-with-sbom
  materials:
    - ref: artifact-signed
runner:
  type: GITHUB_ACTION
